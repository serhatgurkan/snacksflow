import { View } from "react-native";
import styled from "styled-component";
import { typography, space, color, flexbox } from "styled-system";

const Box = styled(View)(typography, space, color, flexbox);

export default Box;
